name: Keep Replit Alive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping: 
    runs-on: ubuntu-latest
    steps:
      - run: curl -sL https://workspace.astor-stroy.replit.dev
 
